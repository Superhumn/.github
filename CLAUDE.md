Keep all changes minimal and focused. Do NOT refactor unrelated code. Do NOT delete files without explicit instruction. Run linters and tests before committing. Write clear commit messages.

After pushing a branch, automatically create a PR without asking for confirmation.
